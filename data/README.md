# Data Directory

This directory stores datasets versioned with DVC.

- `raw/` - Raw datasets (tracked by DVC)
- `processed/` - Processed datasets (generated by DVC pipeline)

Files are stored remotely and pulled via `dvc pull`.

